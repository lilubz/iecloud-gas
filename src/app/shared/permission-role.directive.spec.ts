import { PermissionRoleDirective } from './permission-role.directive';

describe('PermissionRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionRoleDirective();
    expect(directive).toBeTruthy();
  });
});
